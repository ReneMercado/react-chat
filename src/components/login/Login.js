import React, { Component } from 'react';


class Login extends React {

    state = {
        toggleAuth: false
    }

    render(){

        let temp = null;

        return(


            <button>Auth</button>
            <button>Crear Usuario</button>
        );
    }
}